import React from 'react';

const ScrollBar = () => {
    return (
        <div style={{ position: 'fixed', right: 0, top: 0, width: '5px', height: '100%', background: 'rgba(255,255,255,0.1)', zIndex: 9999 }}>
            {/* Scroll Bar Placeholder */}
        </div>
    );
};

export default ScrollBar;
